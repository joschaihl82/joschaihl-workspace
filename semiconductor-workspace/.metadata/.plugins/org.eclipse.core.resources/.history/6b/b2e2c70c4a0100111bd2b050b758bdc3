# Additional clean files
cmake_minimum_required(VERSION 3.16)

if("${CONFIG}" STREQUAL "" OR "${CONFIG}" STREQUAL "Release")
  file(REMOVE_RECURSE
  "CMakeFiles/QtPaintClone_autogen.dir/AutogenUsed.txt"
  "CMakeFiles/QtPaintClone_autogen.dir/ParseCache.txt"
  "QtPaintClone_autogen"
  )
endif()
