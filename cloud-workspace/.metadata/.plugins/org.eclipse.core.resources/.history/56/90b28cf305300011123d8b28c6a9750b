# Additional clean files
cmake_minimum_required(VERSION 3.16)

if("${CONFIG}" STREQUAL "" OR "${CONFIG}" STREQUAL "Release")
  file(REMOVE_RECURSE
  "CMakeFiles/google_drive_sync_demo_autogen.dir/AutogenUsed.txt"
  "CMakeFiles/google_drive_sync_demo_autogen.dir/ParseCache.txt"
  "google_drive_sync_demo_autogen"
  )
endif()
