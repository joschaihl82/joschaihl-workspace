# Additional clean files
cmake_minimum_required(VERSION 3.16)

if("${CONFIG}" STREQUAL "" OR "${CONFIG}" STREQUAL "Release")
  file(REMOVE_RECURSE
  "CMakeFiles/gitpush_autogen.dir/AutogenUsed.txt"
  "CMakeFiles/gitpush_autogen.dir/ParseCache.txt"
  "gitpush_autogen"
  )
endif()
