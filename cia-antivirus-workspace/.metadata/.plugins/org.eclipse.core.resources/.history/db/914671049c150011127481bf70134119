# Additional clean files
cmake_minimum_required(VERSION 3.16)

if("${CONFIG}" STREQUAL "" OR "${CONFIG}" STREQUAL "Release")
  file(REMOVE_RECURSE
  "CMakeFiles/cia-antivirus_autogen.dir/AutogenUsed.txt"
  "CMakeFiles/cia-antivirus_autogen.dir/ParseCache.txt"
  "cia-antivirus_autogen"
  )
endif()
