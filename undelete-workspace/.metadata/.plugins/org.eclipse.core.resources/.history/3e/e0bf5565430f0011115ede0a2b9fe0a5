# Additional clean files
cmake_minimum_required(VERSION 3.16)

if("${CONFIG}" STREQUAL "" OR "${CONFIG}" STREQUAL "Release")
  file(REMOVE_RECURSE
  "CMakeFiles/ext4recover_autogen.dir/AutogenUsed.txt"
  "CMakeFiles/ext4recover_autogen.dir/ParseCache.txt"
  "ext4recover_autogen"
  )
endif()
