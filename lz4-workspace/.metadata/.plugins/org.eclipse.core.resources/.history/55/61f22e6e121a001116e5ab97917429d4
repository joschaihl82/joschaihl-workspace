# Additional clean files
cmake_minimum_required(VERSION 3.16)

if("${CONFIG}" STREQUAL "" OR "${CONFIG}" STREQUAL "Release")
  file(REMOVE_RECURSE
  "CMakeFiles/lz4_packer_autogen.dir/AutogenUsed.txt"
  "CMakeFiles/lz4_packer_autogen.dir/ParseCache.txt"
  "lz4_packer_autogen"
  )
endif()
